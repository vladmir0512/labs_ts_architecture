### tsc - переводит в js 
`tsc lab_1.ts` создает файл lab_1.js


### tsc -p билдит .js и .js.map или Ctrl+Shift+B -> tsconfig.json

Under the covers, we run the TypeScript compiler as a task. The command we use is: tsc -p .